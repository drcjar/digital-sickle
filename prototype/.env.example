# Copy to .env and adjust as needed. The defaults are fine for local use.
NODE_ENV=development
PORT=3000
# Set strong random values in any non-local environment:
SESSION_SECRET=change-me
CSRF_SECRET=change-me
# Optional overrides:
# DB_PATH=./data/app.db
# UPLOADS_DIR=./data/uploads
